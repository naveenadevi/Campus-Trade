<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Your Orders</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
</head>
<body class="bg-gray-50 text-gray-900 min-h-screen py-10 px-4">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-3xl font-semibold mb-8">🛍️ Your Orders</h1>

    <% if (orders.length === 0) { %>
      <div class="bg-white p-6 rounded-lg shadow text-center text-gray-600">
        You have not placed any orders yet.
      </div>
    <% } else { %>
      <% orders.forEach(order => { %>
        <div class="bg-white p-6 rounded-lg shadow mb-6">
          <div class="flex justify-between mb-2">
            <p class="text-sm text-gray-500">Order Date: <%= new Date(order.createdAt).toLocaleDateString() %></p>
            <p class="text-sm text-gray-500">Order ID: <%= order._id %></p>
          </div>

          <div class="mb-2">
            <p class="text-gray-700 font-medium">Shipping Details</p>
            <p><%= order.userDetails.name %></p>
            <p><%= order.userDetails.address %></p>
            <p><%= order.userDetails.phone %></p>
          </div>

          <div class="mb-2">
            <p class="text-gray-700 font-medium">Payment Mode: <%= order.paymentMode %></p>
            <p class="text-gray-700">Total Amount: ₹<%= order.totalAmount %></p>
          </div>
          <a href="/invoice/<%= order._id %>" class="text-blue-600 underline text-sm hover:text-blue-800 ml-2">
  View Invoice
</a>


          <div>
            <p class="text-gray-700 font-medium mb-2">Items:</p>
            <ul class="list-disc list-inside text-sm text-gray-800">
              <% order.items.forEach(item => { %>
                <li><%= item.title %> – ₹<%= item.price %> x <%= item.quantity %></li>
              <% }) %>
            </ul>
          </div>
        </div>
      <% }) %>
    <% } %>
  </div>
  <div class="text-center mt-6">
  <a href="/home" class="inline-block bg-blue-600 text-white px-6 py-2 rounded-lg text-sm hover:bg-blue-700 transition">
    🏠 Back to Home
  </a>
</div>

</body>
</html>
