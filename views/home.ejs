<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>🛍️ Campus Trade - Home</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
  <% if (added) { %>
    <script>
      alert("✅ Product added to cart!");
    </script>
  <% } %>
</head>

<body class="bg-gradient-to-r from-purple-200 via-indigo-200 to-blue-200 min-h-screen">  

  <!-- Header -->
  <header class="bg-white p-4 md:px-8 flex flex-wrap md:flex-nowrap justify-between items-center shadow">
    <div>
      <h1 class="text-xl font-semibold text-gray-900">Campus Trade</h1>
    </div>
    <div class="flex flex-wrap gap-3 items-center mt-2 md:mt-0">
      <form action="/home" method="get" class="flex">
        <input type="text" name="search" placeholder="🔍 Search products..." value="<%= searchQuery || '' %>" 
          class="px-3 py-2 rounded-l-md border border-gray-300 focus:outline-none focus:ring-2 focus:ring-blue-500 text-sm w-52">
        <button type="submit" class="bg-blue-600 text-white px-4 rounded-r-md text-sm hover:bg-blue-700 transition">Search</button>
      </form>
      <a href="/cart" class="bg-black text-white text-sm px-3 py-2 rounded hover:bg-gray-800 transition">🛍️ Cart</a>
      <a href="/orders" class="bg-black text-white text-sm px-3 py-2 rounded hover:bg-gray-800 transition">📜 Orders</a>
      <a href="/my-products" class="bg-black text-white text-sm px-3 py-2 rounded hover:bg-gray-800 transition">My Products</a>
      <a href="/logout" class="bg-black text-white text-sm px-3 py-2 rounded hover:bg-gray-800 transition">🚪 Logout</a>
      <span class="text-sm text-gray-700">👤 <%= userName %></span>
    </div>
  </header>

  <!-- Section Title -->
  <h2 class="text-2xl font-semibold text-center text-gray-800 mt-10 mb-4">🔥 Best Selling Products</h2>

  <!-- Products Grid -->
  <section class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-6 px-6 pb-12">
    <% if (!products || products.length === 0) { %>
      <p class="col-span-full text-center text-gray-600">No products found.</p>
    <% } else { %>
      <% products.forEach(product => { %>
        <div class="bg-white rounded-xl shadow p-4 text-center">
          <img src="/<%= product.img || 'images/default.png' %>" alt="<%= product.title %>" class="w-full h-48 object-contain rounded mb-3 bg-gray-100">
          <h3 class="text-lg font-medium text-gray-900 mb-1"><%= product.title %></h3>
          <p class="text-gray-700 text-sm mb-1">Price: ₹<%= product.price %></p>
          <p class="text-sm font-medium <%= product.stock === 0 ? 'text-red-600' : 'text-green-600' %> mb-2">
            <%= product.stock === 0 ? 'Out of Stock' : 'In Stock: ' + product.stock %>
          </p>
          <% if (product.stock > 0) { %>
            <form action="/add-to-cart/<%= product._id %>" method="POST">
              <button class="bg-blue-600 text-white text-sm px-4 py-2 rounded hover:bg-blue-700 transition">Add to Cart</button>
            </form>
          <% } else { %>
            <button disabled class="bg-gray-400 text-white text-sm px-4 py-2 rounded cursor-not-allowed">Out of Stock</button>
          <% } %>
        </div>
      <% }); %>
    <% } %>
  </section>

</body>
</html>
