<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= product ? 'Edit' : 'Add' %> Product | Campus Trade</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-white text-gray-900 min-h-screen flex items-center justify-center p-6">
  <div class="max-w-xl w-full bg-white shadow-xl rounded-lg p-8">
    <h1 class="text-2xl font-bold mb-6 text-center"><%= product ? 'Edit' : 'Add' %> Product</h1>
    <form action="<%= product ? '/admin/edit/' + product._id : '/admin/add-product' %>" method="POST" enctype="multipart/form-data" class="space-y-4">
      <div>
        <label class="block text-sm font-medium mb-1">Title</label>
        <input type="text" name="title" required value="<%= product?.title || '' %>" class="w-full px-4 py-2 border rounded-md" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1">Price</label>
        <input type="number" name="price" required value="<%= product?.price || '' %>" class="w-full px-4 py-2 border rounded-md" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1">Category</label>
        <input type="text" name="category" required value="<%= product?.category || '' %>" class="w-full px-4 py-2 border rounded-md" />
      </div>
      <div>
        <label class="block text-sm font-medium mb-1">Stock</label>
        <input type="number" name="stock" required value="<%= product?.stock || 0 %>" class="w-full px-4 py-2 border rounded-md" />
      </div>
      <% if (product) { %>
        <input type="hidden" name="oldImg" value="<%= product.img %>" />
      <% } %>
      <div>
        <label class="block text-sm font-medium mb-1">Product Image</label>
        <input type="file" name="img" class="w-full" />
      </div>
      <div class="flex justify-between">
        <a href="/admin/dashboard" class="text-sm text-gray-600 hover:underline">← Back</a>
        <button type="submit" class="bg-black text-white px-5 py-2 rounded hover:bg-gray-800 transition">
          <%= product ? 'Update' : 'Add' %> Product
        </button>
      </div>
    </form>
  </div>
</body>
</html>
