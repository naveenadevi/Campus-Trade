<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Cart | Campus Trade</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    body { font-family: 'Inter', sans-serif; }
  </style>
</head>
<body class="bg-white text-gray-900 min-h-screen">
  <div class="max-w-4xl mx-auto px-4 py-10">
    <h1 class="text-3xl font-semibold mb-8">🛒 Your Cart</h1>

    <% if (!cart || cart.length === 0) { %>
      <p class="text-gray-500">Your cart is empty.</p>
    <% } else { %>
      <% cart.forEach(item => { %>
        <div class="flex justify-between items-center p-4 bg-white shadow rounded mb-4">
          <div>
            <h2 class="text-lg font-semibold"><%= item.title %></h2>
            <p class="text-sm text-gray-600">Price: ₹<%= item.price %></p>
          </div>

          <div class="flex items-center space-x-2">
            <form action="/cart/decrease/<%= item.productId %>" method="POST">
              <button type="submit" class="bg-gray-200 px-2 py-1 rounded text-lg">−</button>
            </form>

            <span class="mx-2 font-medium"><%= item.quantity %></span>

            <form action="/cart/increase/<%= item.productId %>" method="POST">
              <button type="submit" class="bg-gray-200 px-2 py-1 rounded text-lg">+</button>
            </form>
          </div>

          <div class="text-right">
            <p class="text-sm font-medium">Total: ₹<%= item.quantity * item.price %></p>

            <form action="/cart/remove/<%= item.productId %>" method="POST">
              <button type="submit" class="text-red-600 underline text-sm mt-1">Remove</button>
            </form>
          </div>
        </div>
      <% }); %>

      <h2 class="text-xl font-bold mt-6">
        Grand Total: ₹<%= cart.reduce((sum, item) => sum + item.price * item.quantity, 0) %>
      </h2>

      <form action="/checkout" method="GET">
        <button class="mt-6 bg-black text-white px-4 py-2 rounded hover:bg-gray-800">
          Proceed to Checkout
        </button>
      </form>
    <% } %>
  </div>
</body>
</html>
